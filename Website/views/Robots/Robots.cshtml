User-agent: *
Disallow: /views/

@{
    Response.ContentType = "text/plain";
    var path = !string.IsNullOrWhiteSpace(Blog.BlogPath) ? "/" + Blog.BlogPath : "";
}

sitemap: @(Request.Url.Scheme + "://" + Request.Url.Authority)@path/sitemap.xml